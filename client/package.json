{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./scripts/build.cjs",
    "serveDev": "esbuild --bundle src/app.ts --outfile=static/bundle-microunivers.js --servedir=static",
    "watchDev": "esbuild --bundle src/app.ts --outfile=static/bundle-microunivers.js --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pixi/tilemap": "^3.2.2",
    "esbuild": "^0.14.30",
    "pixi.js": "^6.3.0"
  }
}
